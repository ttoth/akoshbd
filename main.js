// Class
const IterativeRotationCipher = {};

IterativeRotationCipher.encode = function(n,str){
	//actually, you won't need this today...
	return str;
};

IterativeRotationCipher.decode = function(str){
	//your code goes here. you can do it!
	return str;
};

// Main
let cipheredMessage = `10 aspjki-zuááa l oomszlógzáá orKlpaotnnkT`;
console.log(IterativeRotationCipher.decode(cipheredMessage));